// src/lib/firebase/clientApp.ts
// THIS FILE IS INTENTIONALLY MODIFIED FOR PROTOTYPE/DEMO MODE.
// IT DOES NOT CONNECT TO FIREBASE.

// To re-enable Firebase connectivity:
// 1. Restore the original content of this file.
// 2. Update AuthContext.tsx to use the real Firebase auth instead of mock auth.
// 3. Ensure your .env.local file has the correct Firebase credentials.

export const app = null;
export const db = null;
export const auth = null;
export const storage = null;
export const firebaseInitializedSuccessfully = false;
